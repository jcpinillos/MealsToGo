import { colors } from './colors';
import { space, spaceArray, lineHeights } from './spacing';
import { sizes } from './sizes';
import { fonts, fontWeights, fontSizes } from './fonts';

export const theme = {
    colors,
    space,
    spaceArray,
    lineHeights,
    sizes,
    fonts,
    fontSizes,
    fontWeights,
};
